# 智能体系统 - 工具清单

> 小锦可调用的代理(Agent)相关工具和团队协作能力

---

## 🤖 Agent 工具总览

### 1. `agent` — 派生后台代理

创建并启动一个后台智能代理，用于执行复杂或并行的任务。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `description` | string | ✅ | 简短的任务描述 |
| `prompt` | string | ✅ | 给代理的完整提示词 |
| `subagent_type` | string | ❌ | 代理类型（如 `general-purpose`, `Explore`, `worker`） |
| `model` | string | ❌ | 指定使用的模型 |
| `command` | string | ❌ | 覆盖启动命令 |
| `team` | string | ❌ | 可选的团队标识 |
| `mode` | string | ❌ | 代理模式：`local_agent`, `remote_agent`, `in_process_teammate` |

**使用场景：**
- 需要并行处理多个任务
- 复杂任务拆分为子任务
- 长时间运行的分析工作

---

### 2. `send_message` — 向代理发送消息

向正在运行的代理任务发送后续消息，动态调整任务方向。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id` | string | ✅ | 目标代理任务 ID |
| `message` | string | ✅ | 要发送的消息内容 |

**使用场景：**
- 任务进行中补充信息
- 根据中间结果调整方向
- 追问或补充细节

---

### 3. `task_create` — 创建后台任务

创建后台 Shell 或本地 Agent 任务。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | ❌ | 任务类型：`local_bash` 或 `local_agent` |
| `description` | string | ✅ | 简短任务描述 |
| `command` | string | ❌ | Shell 命令（local_bash 类型） |
| `prompt` | string | ❌ | 提示词（local_agent 类型） |
| `model` | string | ❌ | 指定模型 |

**使用场景：**
- 运行耗时的 Shell 命令
- 启动后台分析任务

---

### 4. `task_get` — 获取任务详情

查询后台任务的详细信息。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id` | string | ✅ | 任务标识符 |

---

### 5. `task_list` — 列出后台任务

列出所有后台任务，支持按状态过滤。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `status` | string | ❌ | 可选状态过滤（如 `running`, `completed`, `failed`） |

---

### 6. `task_stop` — 停止后台任务

终止指定的后台任务。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id` | string | ✅ | 任务标识符 |

---

### 7. `task_output` — 读取任务输出

读取后台任务的输出日志。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id` | string | ✅ | 任务标识符 |
| `max_bytes` | int | ❌ | 最大读取字节数，默认 12000 |

---

### 8. `task_update` — 更新任务

更新任务描述、进度或状态备注。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id` | string | ✅ | 任务标识符 |
| `description` | string | ❌ | 更新任务描述 |
| `progress` | int | ❌ | 进度百分比 (0-100) |
| `status_note` | string | ❌ | 简短的状态备注 |

---

## 👥 团队管理工具

### 9. `team_create` — 创建团队

创建轻量级内存团队，用于组织代理任务。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | 团队名称 |
| `description` | string | ❌ | 团队描述 |

---

### 10. `team_delete` — 删除团队

删除指定的内存团队。

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | 团队名称 |

---

## 📐 模式切换工具

### 11. `enter_plan_mode` — 进入计划模式

切换到计划模式，用于在编码前进行详细规划和讨论。

---

### 12. `exit_plan_mode` — 退出计划模式

从计划模式切换回默认模式，开始执行代码。

---

## 🔄 典型工作流

```
┌─────────────────────────────────────────────────┐
│                    工作流                         │
├─────────────────────────────────────────────────┤
│                                                  │
│  1. enter_plan_mode    →  进入规划阶段           │
│       ↓                                          │
│  2. agent()           →  派生子代理              │
│       ↓                                          │
│  3. task_list()       →  查看任务状态            │
│       ↓                                          │
│  4. send_message()    →  动态调整方向            │
│       ↓                                          │
│  5. task_output()     →  检查结果                │
│       ↓                                          │
│  6. task_update()     →  更新进度                │
│       ↓                                          │
│  7. exit_plan_mode    →  退出规划，执行代码       │
│                                                  │
└─────────────────────────────────────────────────┘
```

## 📝 使用示例

### 派生一个分析代理

```python
agent(
    description="分析项目架构",
    prompt="请分析当前项目的代码结构和依赖关系，输出详细报告",
    subagent_type="worker",
    model="gpt-4"
)
```

### 向运行中的代理追加信息

```python
send_message(
    task_id="agent_001",
    message="补充一下：重点关注数据库相关的模块"
)
```

### 创建团队并行工作

```python
# 创建团队
team_create(name="frontend-team", description="前端开发小组")

# 派生任务
agent(
    description="编写 API 接口",
    team="frontend-team",
    prompt="..."
)
```

---

*小锦 — 让编程变得更简单 🚀*
